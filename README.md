# es6-angular-webpack

### Prerequisites
 - Install webpack globally `npm install webpack -g`;
 - Install Node (Currently using 6.9.2 version);

### To run the project
 - run `npm install`
 - run `npm run start`
